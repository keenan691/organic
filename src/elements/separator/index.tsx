import React from 'react'
import { TouchableOpacity } from 'react-native'
import styles from './styles'

function Separator () {
  return <TouchableOpacity style={styles.separator} />
}

export default Separator
