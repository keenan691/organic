import React from "react";
import { View, Text } from 'react-native'
import { connect } from "react-redux";
// import styles from './styles/PlaceStyles'

const isPlaceMarked = (place) => {
  // Checks if
}


const PlaceMarker = (props) => {
  return (
    <View style={styles.container}>
      <Text style={styles.titleText}>MarkPlaceSwitch</Text>
    </View>
  )
}

const Place = (props) =>  (
  <View></View>
)

Place.propTypes = {
}

Place.defaultProps = {
}

export default Place;
